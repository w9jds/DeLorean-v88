service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow write: if false;
      allow read: if request.auth.uid == uid || get(/user/$(request.auth.uid)).data.admin == true;
    }
  }
}